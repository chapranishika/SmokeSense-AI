fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
websockets==13.1
numpy>=2.0.0
scipy>=1.14
python-multipart==0.0.12
anthropic==0.39.0
httpx==0.27.2
bcrypt==4.2.1
python-dotenv==1.0.1
pytest==8.3.4

# Real Bayesian inference (backend/bayesian_inference.py). The API
# degrades gracefully to a Gaussian-approximation fallback if these
# aren't installed or data/artifacts/bayesian_vi_params.npz is missing,
# but install them for genuine NumPyro posterior sampling on /predict.
jax==0.10.2
jaxlib==0.10.2
numpyro==0.21.0
